#include "Pieces.cpp"

void Board::setUpBoard(){
	
	for(int i = 0; i < 8; i++){
		for(int j = 0; j < 8; j++){
			
		}
	}
	
	/* for(int index = 0; index < 24; index++){
		if(index < 8){
			if((index%2) == 1){
				pieceArray[index] = new Normal(index, 0, true);
			}
		}
		
		else if(index < 16){
			if((index%2) == 0){
				pieceArray[index] = new Normal(index, 1, true);
			}
		}
		
		else if(index < 24){
			if((index%2) == 1){
				pieceArray[index] = new Normal(index, 2, true);
			}
		}
	} */
}